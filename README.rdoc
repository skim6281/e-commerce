# E-Commerce

## Usage
- Setup
  ```
  git clone https://github.com/skim6281/e-commerce.git
  bundle install
  npm install
  webpack
  bundle exec rails s
  ```
- Go to [localhost:3000](localhost:3000)
